package com.joffrey.search.binary.exercices;

public class Sqrt {

    public static int of(int target) {
        // int low = 0
        // int high = target

        // while ( * condition )
        // low < high because of low = mid + 1;
        // low = high because of high = mid - 1;)

            // int mid is equals to (low + high) / 2
            // int square is equals to mid²

            // if square is equals to target
                // found it ! we return mid
            // else if square is < target
                // low = mid + 1
            // else
                // high = mid - 1

        // we found nothing !
        return -1;
    }

}
