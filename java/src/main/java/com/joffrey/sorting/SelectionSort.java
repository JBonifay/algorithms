package com.joffrey.sorting;

import java.util.List;

public class SelectionSort {

    public List<Integer> selectionSort(List<Integer> inputList) {
        // We create a list that will receive values
        // List<Integer> newList = new ArrayList<>(inputList.size());

        // We iterate over inputList
            // int smallest will be the smallest value found in findSmallest(inputList)
            // we add to newList the smallest value from inputList
            // then we remove the smallest value from inputList

        // we return newList
        return null;
    }

    private int findSmallest(List<Integer> inputList) {
        // int smallest is inputList[0] first
        // int smallestIndex is 0 as well
        // we iterate over inputList
            // if inputList[i] < smallest
                // smallest is assigned to inputList[i]
                // smallestIdx is equals to i
        // return smallestIndex
        return -1;
    }

}

